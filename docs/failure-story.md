# Failure Story

- 2023-02-09: Tried to load the journal into a browser preview, but the file path kept breaking because the portal mock is just a markdown note.
- Next step is to add a tiny loader script that can read the journal text and push it into a JSON bundle for the UI.
